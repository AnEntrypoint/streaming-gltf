{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EP_progressive_lod mesh descriptor",
  "type": "object",
  "description": "Per-primitive set of LOD variants, ordered coarse-to-fine by the client after load.",
  "properties": {
    "meshIndex": {
      "type": "integer",
      "minimum": 0,
      "description": "Index into the glTF meshes array."
    },
    "primIndex": {
      "type": "integer",
      "minimum": 0,
      "description": "Index into the mesh's primitives array."
    },
    "lods": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "lod.EP_progressive_lod.schema.json" }
    }
  },
  "required": ["meshIndex", "primIndex", "lods"],
  "additionalProperties": false
}
