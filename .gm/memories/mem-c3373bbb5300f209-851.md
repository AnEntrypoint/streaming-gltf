---
key: mem-c3373bbb5300f209-851
ns: default
created: 1783024637046
updated: 1783024637046
---

## Resolved mutable: git-push-rebase-identical-content-conflict

Root cause confirmed via git merge-file -p direct 3-way test: adjacent-line edits (my line 4, origin's line 3, no blank-line separator between them) get grouped into ONE diff hunk by git's line-based algorithm even when the final resulting text has zero semantic overlap -- this is git's real hunk-collision behavior, not a plugkit tooling bug. Resolved by git_reset --soft to origin/master (making HEAD == origin/master, zero rebase distance) before each commit that touches this file, so the commit applies with no replay/hunk-diff needed at all. Landed via two commits: b91ce65 (occlusion-culling feature, package.json untouched to avoid the collision) then 2a15ef7 (description-only fix, committed directly on the then-current origin tip). Both pushed successfully via git_finalize.
