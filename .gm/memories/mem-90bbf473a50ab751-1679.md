---
key: mem-90bbf473a50ab751-1679
ns: default
created: 1779841423089
updated: 1779841423089
---

gltf-progressive perf session COMPLETE state (commits through 488155f). Net: 63 -> 70 FPS median @500 distinct (RTX3060 CHANNEL=chrome harness, far-heavy visible498). ALL agent-listed non-WebGPU levers now pursued: (1) far-LOD decimation to ~400 tris via spatial-grid clustering at load [SHIPPED, 756k->333k tris]; (2) LOD-distance FPS control replacing ceiling [SHIPPED, no oscillation]; (3) THREE.BatchedMesh far tier [SHIPPED, 742->6 draws, +7fps, the off-screen was a far-camera coverage artifact]; (4) per-VERTEX gamma decode (moved pow(vColor,2.2) from fragment to <color_vertex>) on BatchedFarTier+vcMaterial [SHIPPED, witnessed correct colors]; (5) per-frame distance SORT skip when camera-still+no-movers+same-N [SHIPPED, tiers identical, compounds with static fast-skip]; (6) sub-pixel model cull pool._subPixelCullPx default 2px [IMPLEMENTED but BOUNDED: demo grid only ~46u wide so models never go sub-pixel even at far camera + static fast-skip shadows it; correct+available for sparse huge worlds, ~0 impact here]; (7) far texture-LOD LinearFilter/no-mipmap [VERIFIED already shipped, model-pool _applyTexLod farLod wantIdx>=3]; (8) per-entity frustum-check interval [VERIFIED already adaptive via _dynamicFrustumCheckInterval 5-10 frames]; (9) GPU-instance-texture [MOOT: BatchedFarTier supersedes the InstancedSlot/InstancedBatch path entirely for the far tier, so the data-texture path is dead code in shipped config; left off, no fix needed]. Measurement discipline reaffirmed: matched-camera coverage compare before concluding render bugs; force p.renderer.render() before readPixels; measure-fps.mjs steady-state is authoritative vs mid-settle ad-hoc samples.
