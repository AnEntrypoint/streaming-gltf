---
key: mem-423d0202823f57a2-1442
ns: default
created: 1779839126128
updated: 1779839126128
---

gltf-progressive far-tier perf session FINAL state (commits a6c8500..de5b2ca): far/unskinned LODs decimated at load to <=400 tris via dependency-free spatial-grid vertex clustering _clusterDecimate (lod-worker.js + model-pool.js, descend res 48->2 accepting finest grid under cap — ascending was the straggler bug). ALL 494 far LODs <=400 tris (was ~6500, max 104064), drawn tris 756k->333k. FPS control drives _lodDistanceScale (0.15..1.6 multiplier on _pickMeshLod screenPx) NOT the ceiling -> no tier oscillation, settles stable (commit be58e10). NET RESULT measured (RTX3060 CHANNEL=chrome harness, far-heavy visible498/far442): 63-65 FPS = same-or-slightly-better than the 63.3 pre-session baseline, now with stable LOD control + correct vertex colors (getX/getY denormalize — raw .array copy made far models WHITE). DRAW-CALL-BOUND now (494 far draws at fixed-cam all-visible -> ~40fps floor): THREE.BatchedMesh collapses 742->6 draws (PROVEN browser-341) but the BatchedFarTier adapter renders instances OFF-SCREEN (coverage 2%) = per-instance transform/cull bug, left OFF (useBatchedFarTier=false). Fixing that adapter is the next real win for the all-visible draw-bound case. GPU-instance-texture also OFF (broke at scale). CRITICAL measurement lesson: ad-hoc fixed-cam samples during controller-settle showed 26-40fps and looked like regressions but were transient; measure-fps.mjs steady-state harness is authoritative = 63-65.
