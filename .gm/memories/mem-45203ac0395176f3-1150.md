---
key: mem-45203ac0395176f3-1150
ns: default
created: 1780591140705
updated: 1780591140705
---

streaming-gltf DRACO RUNTIME DECODER swapped WASM -> mrdoob draco.js (pure JS), witnessed byte-identical. WHY: removes runtime gstatic CDN + .wasm fetch (model-pool.js used setDecoderPath(gstatic/1.5.7)). VENDORED: examples/local-progressive/draco-loader.js = mrdoob build/DRACOLoader.min.js (110KB, imports bare three, exports DRACOLoader, Apache-2.0; NOT npm draco.js which is yindaheng98 WASM wrapper). CHANGES: model-pool.js:22 import DRACOLoader from ./draco-loader.js; :36 _sharedDracoLoader=new DRACOLoader() no setDecoderPath; both _makeLoader paths use it. lod-worker.js: sibling LODs carry BOTH draco+meshopt so worker GLTFLoader (had only MeshoptDecoder) now also setDRACOLoader - worker fetches ./draco-loader.js, regex-rewrites from"three" -> esm.sh/three@0.170.0 (workers have no importmap), imports via blob URL. BAKE encoder stays draco3dgltf WASM (dev-time Node, draco.js is decode-only). WITNESS browser-400: posCount 9302==9302 vs three WASM, bboxMaxDelta 0. browser-401 worker: decodeOk true posCount 11634 no fallback warn. KEY GOTCHA: module workers cant resolve bare three import in a vendored file -> blob-URL rewrite pattern.
