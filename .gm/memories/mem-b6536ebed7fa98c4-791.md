---
key: mem-b6536ebed7fa98c4-791
ns: default
created: 1779890133488
updated: 1779890133488
---

MEASUREMENT TRAP (streaming-gltf): the gm `browser` verb leaves its Chrome session ALIVE after the script returns (session_id increments each call; 34 chrome.exe procs accumulated across a few witness dispatches). Multiple live GPU contexts contend and tank measure-fps.mjs numbers catastrophically (1000-entity median fell 83->19 FPS, visible 956->768, purely from contention). ALWAYS `taskkill //IM chrome.exe //F` immediately before running CHANNEL=chrome measure-fps.mjs, and treat any measurement taken while browser-verb sessions are open as INVALID. The `browser` verb also has a hard ~14000ms execution cap regardless of requested timeout_ms, so it cannot spawn+quiesce 1000 entities — use it only for quick invariant witnesses (<14s), and use measure-fps.mjs for steady-state FPS.
