---
key: mem-d7d846b25517d764-1643
ns: default
created: 1779909117937
updated: 1779909117937
---

streaming-gltf is now an SDK + has a GitHub Pages demo (commit 09845ab, master, pushed). SDK: index.js barrel exports { ModelPool, BatchedFarTier }; package.json dropped private:true, has main/module/exports map ('.', './model-pool', './batched-far-tier'), files whitelist, sideEffects:false; three AND @pixiv/three-vrm are peerDependencies (BOTH are hard ESM imports in model-pool.js:19/23 — a bare node import fails to resolve them, which is expected; the browser importmap/bundler provides them). PAGES DEMO: https://anentrypoint.github.io/streaming-gltf/ via .github/workflows/deploy-pages.yml (publishes examples/local-progressive/, stress.html->index.html, code-only). ASSETS ARE HOSTED SEPARATELY: the baked models live in repo C:\dev\assets (github.com/anEntrypoint/assets) served at https://anentrypoint.github.io/assets/ — specifically streaming/output_*/model.progressive.glb (934 dirs, already pushed+live, 200). The host serves manifest.baked.json (category->[{name,path,thumb,baked}] where baked='streaming/output_xxx/model.progressive.glb') with Access-Control-Allow-Origin:*. stress.js now: ASSET_BASE defaults to that host, fetches manifest.baked.json, flattens to baked-path URLs, spawns them cross-origin; ?assets=<url> overrides; ?assets=local uses serve.mjs /assets-list.json + relative output_* (dev). Witnessed both modes load models (remote 200 cross-origin, local 200). TODO for user: enable repo Settings->Pages->Source 'GitHub Actions' once to activate deploy. To regenerate/add baked assets, bake into C:\dev\assets/streaming/output_* and push that repo (its own deploy-pages.yml copies streaming/* into docs/).
