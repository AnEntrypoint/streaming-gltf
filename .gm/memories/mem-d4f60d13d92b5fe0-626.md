---
key: mem-d4f60d13d92b5fe0-626
ns: default
created: 1782643664468
updated: 1782643664468
---

gm OPS LESSON (generalizable): heavy CPU/memory work (mesh clustering on tens-of-thousands of verts, basis/KTX2 encode, large geometry builds) run via the exec_js VERB executes in the plugkit watcher's process and OOM/aborts it mid-verb ('watcher died mid-verb; side effects partial'), and the stuck in/exec_js/N.txt re-runs every restart = crash loop. FIX: run heavy compute as a SEPARATE `node script.mjs` process via Bash (cwd=repo so bare-specifier imports resolve), and rm the stuck exec_js input after a mid-verb death before re-dispatching. Reserve exec_js for light probes (API-signature discovery, small round-trips).
