---
key: mem-0d95fae715fb3a45-1185
ns: default
created: 1779840206268
updated: 1779840206268
---

gltf-progressive BatchedFarTier IS SHIPPED ON and is a WIN (commit faf71cc), correcting earlier wrong notes that called it off-screen/neutral. THREE.BatchedMesh FAR tier (useBatchedFarTier=true, batched-far-tier.js, MeshBasicMaterial unlit vertexColors) collapses all distinct far-asset draws 742->6. After far-LOD decimation (~400 tris each) the scene is DRAW-CALL-BOUND so this pays off: measured RTX3060 CHANNEL=chrome 500 distinct = 68-70 FPS median (vs 63 baseline), peaks 140+ (vs ~92), coverage equal-or-better than per-asset path. The earlier 'renders off-screen coverage 2%' was a MEASUREMENT ARTIFACT: at a far camera 498 tiny far dots genuinely cover ~2% with OR without batching (matched-camera browser-369 vs 366 confirmed identical coverage). LESSON: (1) compare coverage at MATCHED cameras before concluding a render bug; (2) readPixels WITHOUT a forced p.renderer.render() first reads the cleared post-present backbuffer -> false coverage 0; always force-render before readPixels. Net session FPS: 63 -> 68-70 with stable LOD-distance control, far decimation, BatchedMesh, all correct (no white/oscillation). enableDrawCallBatching is skipped when useBatchedFarTier on.
