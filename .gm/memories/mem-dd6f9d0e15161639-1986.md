---
key: mem-dd6f9d0e15161639-1986
ns: default
created: 1779826243216
updated: 1779826243216
---

gltf-progressive stress demo: the ~2s LOD-pop + per-LOD orientation flip (commit adda8ef) had TWO coupled root causes. (1) Ceiling oscillation: TWO controllers wrote _currentCeilingLod in opposite directions forming a limit cycle (period = 120-frame VRAM cooldown ~2s). VRAM safe-path auto-relax fed its own trigger (lower ceiling -> entities to cheap instanced FAR -> _totalBytes drops below safe -> raise back -> bytes climb over critical -> repeat). Fix: removed VRAM auto-relax (VRAM now one-way ratchet, only clamps down), and added FPS anti-windup latch _ceilFloorFromFps (a ceiling level that caused a low-FPS lowering is never auto-raised back into; latch DECAYS after FPS sustained >target+25 for 90 frames since it was set during transient mid-load streaming dip). (2) Orientation flip on tier switch: instanced FAR slot set matrix from bare this.root.matrixWorld, DROPPING the glTF mesh node's local transform that the per-entity textured tier applies via tm.mesh in the cloned scene hierarchy. Fix: _slotWorldMatrix(tm) = root.matrixWorld x tm._meshLocalToRoot (mesh-relative-to-root captured once at bootstrap via root.matrixWorld.invert() x mesh.matrixWorld; null when identity for fast path). 137/500 assets have non-identity node transforms; witnessed slotMismatch:0. (3) minCeil=2 floor: ceiling MUST NOT collapse to 0 or every model freezes at lowest LOD = 'smooth but no LOD switching'; higher LOD index = MORE detail, ceiling caps max detail, distance picks below it. LESSON: any FPS/VRAM controller whose corrective action (LOD ceiling change) itself moves the measured variable across both dead-band edges WILL oscillate regardless of dead-band width or cooldown; the fix is an asymmetric latch that converges, not a wider band. stress.js data-n buttons are only 10/100/500/1000 (no 200/300); use window.__debug.spawnAll() or those exact values. gm browser verb 14s timeout: spawnAll(954)+8s settle+heavy scan EXCEEDS it; use data-n=500 + <=7s warmup + light scan.
