---
key: mem-3fc70e161a53baf7-2149
ns: default
created: 1780054087528
updated: 1780054087528
---

streaming-gltf PERF session (post commit 09845ab SDK-packaging). KEY DISCOVERY: measure-fps.mjs + stress.html default to streaming baked models CROSS-ORIGIN from https://anentrypoint.github.io/assets/ (09845ab changed ASSET_BASE). With NO ?assets= param the far-LOD geometry never reaches geoCache in the measure window, so _getInstancedSlot returns null and EVERY entity renders as its own plain THREE.Mesh (browser-witnessed: 1000 plain mesh, 0 InstancedMesh, 0 BatchedMesh, _batchedFarTier null) = FPS collapse (500=11.97). FIX SHIPPED: measure-fps.mjs now appends ?assets=${ASSETS||'local'} (env override) and cache-bust joins with '&'; benchmarks the real local steady-state engine. With ?assets=local the instanced+BatchedMesh far tier is fully alive (browser-witnessed: _batchedFarTier set, 1 BatchedMesh, instancedSlots>0, only hero/near as plain mesh) and 500 measured 88.61 FPS (visible 498 far 439) even under heavy external GPU contention, consistent with prior clean 500=111. TWO LEVERS SHIPPED+WITNESSED: (1) batched-far-tier.js _normalizeFarGeometry now uploads ONLY position+color (dropped dead normal+uv: MeshBasicMaterial unlit reads neither) -> live far geometry attrs == [color,position], glError 0, ~20 bytes/vertex saved on dominant far tier; (2) model-pool.js ~1422 computes _slotWorldMatrix once into local slotWM reused for setMatrixForSlot + bound-sphere (was 2 calls into shared _slotMtx scratch). lever-far-tri-cap-tune (lower _farTriCap from 400) DEFERRED: needs uncontended A/B + far screenshot. CONTEXT TRAP REPEATED: ~20-34 chrome.exe contexts (mine + external tv8 playwriter automation on C:/dev/tv8/planet.html) contend the GPU and tank measure-fps medians; kill stale chrome before measuring, but tv8 is not ours to kill so absolute uncontended baseline was blocked-external this session. NOTE: the gpu-upload audit subagent's '256MB/20ms per frame DataTexture' claim is FALSE -- _initLerpTexture is ceil(sqrt(maxInstances*2))^2 ~= 91x91 RGBA32F ~133KB. SUPERSEDES the 'no further high-value non-WebGPU lever remains' conclusion: the harness was silently mis-measuring; the real engine still hits ~88-111 FPS @500.
