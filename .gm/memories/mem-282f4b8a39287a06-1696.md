---
key: mem-282f4b8a39287a06-1696
ns: default
created: 1779830212850
updated: 1779830212850
---

gltf-progressive GPU-driven instancing (commit a4fa770, milestone): per-instance transform moved off JS into a float DataTexture (RGBA32F, 4 texels=1 mat4 per instance, NearestFilter+no-mipmaps MANDATORY else GL_INVALID_OPERATION 1282 from float linear filtering). Vertex shader readInstanceMatrix(gl_InstanceID) via texture2D, mvPosition MUST be declared at outer scope (not inside braces) so downstream THREE chunks (fog etc) compile — '#else #include <project_vertex> #endif'. CRITICAL: the demo uses the InstancedBatch path in draw-call-batching.js (pool._geometryBatches, ~98 batches), NOT InstancedSlot in model-pool.js (pool._instancedSlots=0) — both have their OWN copy of _patchInstancedSlotMaterial and both needed the change. Per-batch material must be CLONED when GPU-tex on (shared global FAR material can only bind one batch's instanceTex uniform); each batch is its own InstancedMesh/draw so no draw-call cost. Gated by pool._enableGpuInstanceTex (default on) + USE_GPU_INSTANCE_TEX define. Earlier win same session (commit 4b01bf0): static+_allInstanced+!cameraMoved entities fast-skip _update entirely -> msEntities 23.3ms->1.4ms (~17x) the JS loop was the real bottleneck not GPU. Remaining design (PRD): gpu-lod-selection (per-LOD-bucket visibility, NOT geometry swap - multi-draw unreachable through THREE renderer), gpu-instance-tex-group-regions (contiguous texel block per registered group e.g. players, one batched write+texSubImage per frame for a moving group), gpu-instance-optional-motion. Witness budget: gm browser verb 14s timeout - spawnAll(954)+8s+heavy scan EXCEEDS; use data-n=100/500 + <=6.5s warmup + light scan, console.log flushes only if scan is light.
