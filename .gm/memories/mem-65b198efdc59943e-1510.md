---
key: mem-65b198efdc59943e-1510
ns: default
created: 1779838181240
updated: 1779838181240
---

gltf-progressive FPS controller redesign (commit be58e10): FPS/VRAM control now drives LOD DISTANCE not the global ceiling, eliminating tier oscillation. _pickMeshLod(lods,screenPx,ceilingIdx,use3Lod,lodScale) multiplies screenPx by pool._lodDistanceScale; controller in update() eases _lodDistanceScale in [0.15,1.6] toward target with dead-band (target-6% .. target+10%) + rate-limited proportional steps (every 4th tick). Below target shrinks scale (models drop to lower LOD sooner, each crossing its OWN threshold gradually = no stampede); above grows it. Ceiling left FIXED (no FPS clamp) — that was the oscillation source. Witnessed STABLE: target144/500 -> scale rests 0.15 floor fps 78-95 steady; target30/100 -> scale 1.6 lodHist{lod4:81,lod0:18} full detail fps 124-149 steady. Both settle to constant resting scale. Also this session: far-LOD runtime decimation via dependency-free spatial-grid vertex clustering _clusterDecimate (cap ~400 tris, pool._farTriCap) in lod-worker.js + model-pool.js fallback + cache-set guard — far-heavy fps 48->88-115; MUST use BufferAttribute getters (getX/getY/getZ/getW) when rebuilding normal/color since normals are Int8-normalized + colors Uint8-normalized (raw .array copy -> WHITE far models). ~56/494 dense meshes still bypass decimation (load via a path skipping ensureMeshLod, likely geometry-hash slot reuse). GPU instance-texture default OFF (broke at 500). BatchedMesh far tier default OFF (FPS-neutral, scene is fill/triangle-bound not draw-bound).
