---
key: mem-f71654231656fe16-1368
ns: default
created: 1779830746553
updated: 1779830746553
---

gltf-progressive GPU-instance-texture flicker fix (commit ed25a44): 'lowest LODs disappear/reappear at 500 models' = instance transform texel written too late. _applyLod is ASYNC (awaits ensureMeshLod), so the _update() that triggered it already ran its later per-frame setMatrixForSlot block by the time slot.acquireSlot returns -> transform write deferred to next frame -> if next frame entity is static + camera still, the static fast-skip (commit 4b01bf0) early-returns and the texel is NEVER written -> instance sits at zero matrix = invisible. Old instanceMatrix path masked this (one-time write persisted); data-texture exposed it. FIX: write transform IMMEDIATELY at slot acquisition in _applyLod's wantInstanced branch (slot.setMatrixForSlot(idx, _slotWorldMatrix(tm)) + tm._matrixNeedsUpdate=false), alongside the bound-sphere seed, so it never depends on a deferred block. GENERAL LESSON: any per-frame fast-skip optimization must ensure one-shot state (instance texels, slot transforms) is written SYNCHRONOUSLY at the triggering event, not deferred to a later/next-frame block the skip can bypass. Diagnosis method: count zero vs nonzero instance texels (d[i*16+0]||d[i*16+12]||d[i*16+14]) across pool._geometryBatches[].._instTexData at steady state (inFlight==0); control test = force _cameraMoved=true to disable skip and confirm zeroTexels drops to 0.
