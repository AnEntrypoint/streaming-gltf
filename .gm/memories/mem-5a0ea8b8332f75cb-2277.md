---
key: mem-5a0ea8b8332f75cb-2277
ns: default
created: 1779895027689
updated: 1779895027689
---

streaming-gltf WebGPU + position-update session (commits 5192a92, d2c92d2, master, pushed). GOAL: add WebGPU retaining WebGL, on-GPU position lerping, finish WebGL opts. DELIVERED: (1) O(moving) POSITION-UPDATE system — pool.setTarget(entity,x,y,z,durMs) records a mover in pool._movers; _drainMovers(now) in update() lerps ONLY active movers and pushes via setMatrixForSlot (backend+tier agnostic: far BatchedMesh.setMatrixAt, mid InstancedMesh, hero). Continuous retarget, auto-removed at t>=1, survives LOD/tier switch (record keyed by entity not slot). stress.html 'position updates' toggle + _driveMovers retargets ~3%/frame. Witnessed: entity moved exactly 7.07u to a +5,+5 target, mover 1->0 on completion, zero errors. (2) OPT-IN WebGPU path — createRenderer() factory in stress.js: ?backend=webgpu + navigator.gpu -> dynamic import('three/webgpu') + await init(), else WebGL default (graceful fallback). importmap three/webgpu+three/tsl -> three@0.170.0/build/three.webgpu.js (r0.170 has NO three.tsl.js, 404; TSL re-exported from webgpu build). KEY FINDING (webgpu-probe.mjs): WebGPURenderer DUCK-TYPES on .isMesh flags (sameMeshClass=false but renderStandardMeshOK=true) so it renders the EXISTING standard-material ModelPool scene (BatchedMesh+InstancedMesh+MeshBasicMaterial) with NO TSL rewrite — onBeforeCompile GLSL patches just don't run (three defaults apply). Witnessed: ?backend=webgpu renders 420 entities all tiers ~36fps zero errors. WebGPU SLOWER than WebGL (36 vs 73-144) because the WebGL-only GLSL opts (per-vertex gamma, shader frustum cull) don't carry over -> TSL node-material port deferred as documented future work (webgpu-node-materials, webgpu-lerp-storage). New harnesses: webgpu-probe.{html,mjs}, webgpu-stress-check.mjs. CRITICAL ARCH: default config uses useBatchedFarTier:true so the InstancedSlot/draw-call-batching GPU-instance-texture path is INACTIVE (892/956 visible = far BatchedMesh); the 6-texel in-shader GLSL lerp was deferred (would only serve the dead path) — CPU active-set lerp covers the dominant tier with O(moving). NO WebGL regression: 1000@73fps clean (a 17fps run was 10 leftover Chrome procs contending — ALWAYS taskkill chrome before measuring, esp after WebGPU runs). See [[streaming-gltf PERF session]].
