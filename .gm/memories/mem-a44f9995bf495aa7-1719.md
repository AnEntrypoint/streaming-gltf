---
key: mem-a44f9995bf495aa7-1719
ns: default
created: 1781728830485
updated: 1781728830485
---

streaming-gltf (2026-06-17, commit 7c786a2): textures are now ONE GPU-compressed KTX2 (Basis) per texture, NOT a webp size ladder. Bake (tools/bake-progressive.mjs + bake-streaming.mjs): encodeTextureKTX2 = sharp downscale to BAKE_TEX_SIZE (default 1024, set 512 to compress harder) -> ktx2-encoder encodeToKTX2({isUASTC, needSupercompression, qualityLevel, mipmaps:true, imageDecoder}). KEY: ktx2-encoder in NODE REQUIRES an imageDecoder (sharp ensureAlpha().raw() -> {width,height,data}); the option for block format is isUASTC (boolean), NOT 'mode'. ETC1S (isUASTC:false, scheme 2 BasisLZ) for sRGB color; UASTC (isUASTC:true + needSupercompression Zstd, scheme 1) for linear normal/ORM (detected per material slot getNormal/MetallicRoughness/Occlusion). Wire KHR_texture_basisu (required; KTX2 has no fallback image), strip stale EXT_texture_webp/draco, set extPayload.textures lods=[] (no texture streaming). Runtime (model-pool.js): shared KTX2Loader configured once via _ensureKtx2Loader(renderer) in the ModelPool ctor (setTranscoderPath(new URL('./basis/',import.meta.url)) + detectSupport), _makeLoader attaches it; basis transcoder VENDORED at examples/local-progressive/basis/ (basis_transcoder.js+.wasm, served local, Apache-2.0). Witnessed browser-238: pool.spawn(KTX2 asset) -> map.isCompressedTexture=true, format BC7 (0x8E8C=36492), 10 mips, glError 0. CRITICAL OPS LESSON: heavy basis encode (4096^2) via the exec_js VERB crashes the plugkit watcher (in-process child OOM) and the stale in/exec_js/N.txt re-runs every restart = crash loop (fix: rm the stuck input). ALWAYS run bakes/encodes as separate `node` processes (Bash), never exec_js. ktx2-encoder in optional+devDependencies (bake-time only).
