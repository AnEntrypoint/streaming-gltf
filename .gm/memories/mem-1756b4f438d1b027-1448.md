---
key: mem-1756b4f438d1b027-1448
ns: default
created: 1779839544376
updated: 1779839544376
---

gltf-progressive BatchedFarTier off-screen bug (diagnosed, NOT fixed, useBatchedFarTier left false). Symptom: enabling it collapses draws 742->6 (works!) but pixel coverage drops to 2% — screen near-empty. Diagnosis (browser-351/353): instance matrices ARE correct + spread (sampleXforms [-12.8,0,-17.3] etc, atOrigin 0), all 493 instances getVisibleAt=true, glErr 0; disabling perObjectFrustumCulled did NOT restore coverage (so it's NOT the frustum cull). So matrices right + cull off + visible-flags true, yet geometry renders to ~nothing. Remaining suspects (for follow-up): (a) _normalizeFarGeometry produces degenerate/tiny geometry in the BatchedMesh shared buffer specifically (the SAME decimated geos render fine via per-asset InstancedBatch, so it's BatchedMesh's vertex-buffer packing or the addGeometry schema), (b) the cloned MeshLambertMaterial + BatchedMesh USE_BATCHING chunk interaction, (c) object-level boundingSphere null on the BatchedMesh (frustumCulled=false set, so shouldn't matter). NEXT STEP: add ONE box geometry + ONE real decimated far geo to a fresh BatchedMesh, render both isolated, see which renders — isolates schema/packing vs real-geo issue. Draw collapse is proven (browser-238 pure boxes 10->1 draw rendered fine; browser-341 real 6 draws but empty), so the issue is specifically how real decimated far geos go into the batch. The win when fixed: ~40fps all-visible-draw-bound -> higher (494 draws -> ~1).
