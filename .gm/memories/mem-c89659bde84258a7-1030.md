---
key: mem-c89659bde84258a7-1030
ns: default
created: 1779810113513
updated: 1779810113513
---

gltf-progressive stress demo — THREE big visual bugs fixed (commits a93fa6f, 91893d4): (1) vertex colors white/black = worker denormalizes color to 0-1 Float32 but ships normalized:true; _buildGeometryFromPayload re-normalized -> fix: force normalized=false for Float32Array attrs. (2) LOD never switched with camera = Entity._update gated _pickMeshLod behind if(!tm._instancedSlot) (false GPU-LOD assumption) -> removed gate so instanced entities re-evaluate + can exit instanced tier. (3) textured LODs white = HERO/MID used shared getMaterialForTier pool material which cannot hold per-asset textures AND _applyTexLod only applies when mat===tm.baseMaterial -> fix: textured tiers use tm.baseMaterial. Debug controls added: __debug.inspect(i)/pinLod(n)/unpinLod()/materialPool(bool). REMAINING: some models orientation differs per-LOD = per-asset baking inconsistency (different up-axis/pivot across baked LODs), a DATA problem not a renderer bug; entity #2 quat was consistent across LODs so it affects only specific assets.
