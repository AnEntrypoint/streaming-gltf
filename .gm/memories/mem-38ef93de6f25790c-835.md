---
key: mem-38ef93de6f25790c-835
ns: default
created: 1783455511318
updated: 1783455511318
---

## Resolved mutable: plugkit-update-available-not-applying

cat .gm/exec-spool/.status.json after both force-update attempts: version_drifted:false, version/instance_version/file_version all 0.1.824 -- the wrapper's own drift-detector confirms disk and running instance already agree, so there is nothing locally stale to reconcile. The 0.1.825 gap is upstream (cascade pipeline: push -> release.yml build -> npm publish, per AGENTS.md cascade-pipeline docs, is itself multi-step CI that had not yet landed a fetchable gm-plugkit@latest carrying the 0.1.825 pin at the moment both spool-update commands ran). blockedBy:external -- no further local dispatch changes an unpublished upstream artifact's existence. Non-blocking: transition to=COMPLETE already succeeded (phase=COMPLETE) before this advisory-only warning; it is not a gate.
