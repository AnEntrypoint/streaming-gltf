---
key: mem-b9c425bbb2428f4f-1348
ns: default
created: 1779893244473
updated: 1779893244473
---

## Resolved mutable: gpu-lerp-design

Plan subagent design: DATA MODEL — extend existing per-instance instance-data texture from 4 texels (mat4) to 6 texels/instance: texel0-3=mat4 (rot/scale + base translation c3), texel4=pos0.xyz+startTime(.w), texel5=pos1.xyz+duration(.w). Lerp TRANSLATION ONLY (keep mat4 for rot/scale). SHADER: add uniform uNow (sec, updated once/frame = the ONLY per-frame CPU->GPU write); t=dur>0?clamp((uNow-startT)/dur,0,1):1; lerped=dur>0?mix(pos0,pos1,t):m[3].xyz; m[3].xyz=lerped. t=1 clamp = free completion, pins at pos1 with zero further writes. CPU API: pool.setTarget(entity,[x,y,z],durMs) recomputes current interpolated pos as new pos0 (continuous retarget), writes texels 4-5 + dirty range; setPositionImmediate for dur=0 static. Non-updated entities = ZERO per-frame cost. PER-TIER: MID/InstancedSlot = FULLY FEASIBLE (primary, instance-tex path already exists, default-off behind _enableGpuInstanceTex); FAR/BatchedMesh = PARTIAL (three owns _matricesTexture; recommend CPU-lerp only the active far set, GPU patch is invasive stretch); HERO = skip (tiny, plain Object3D lerp). Per-entity CPU lerp record (Map entity->{pos1,start,dur}) is source of truth that survives LOD/tier switches (reseed new slot texels). WebGPU mirror: StorageInstancedBufferAttribute + positionNode mix instead of DataTexture+GLSL.
