---
key: mem-b0209814789957d7-1864
ns: default
created: 1781712929788
updated: 1781712929788
---

streaming-gltf ARCHITECTURE ASSESSMENT (2026-06-17): user asked to 'voxel arrange highest-LOD polys via spatial index and continuously stream polys into a known fixed buffer' to shrink final model, simplify streaming, delete code. VERDICT: the LITERAL idea (Morton/voxel-sort triangles then truncate the buffer for LOD) does NOT work -- a spatial-order prefix is a spatial PATCH (one corner), not a coarse whole-model approximation. prefix==LOD REQUIRES a refinement hierarchy: (a) Hoppe progressive-mesh vertex-split stream, (b) meshlet cluster-DAG / Nanite-lite, or (c) octree/voxel-cell stream (renders as points/voxels = fidelity change, fine for far tier only). CRITICAL CAVEAT (witnessed prior memory): workload is TRIANGLE/FILL-bound at 500-1000 models, NOT draw-call or size bound (BatchedMesh draw-collapse gave 0 fps) -> continuous streaming is a DOWNLOAD-SIZE/STREAMING-SIMPLICITY/CODE-DELETION win, NOT an FPS win; same on-screen tris = same fps. OVERLAP: _clusterDecimate (lod-worker.js:155, model-pool.js:713) ALREADY voxel-grids bbox res^3 and collapses cells to one rep vertex at LOAD per discrete LOD -- a continuous scheme lifts this to bake time, ordered coarse->fine into one buffer. CURRENT FORMAT replaced: EP_progressive_lod = root GLB (inline LOD0) + N sibling LOD GLB files per asset + _pickMeshLod threshold ladder [80,200,400,800,1400]px + hysteresis + per-LOD ensureMeshLod/_lodWarmQueue fetch. RECOMMENDED smallest-surface variant: bake-time one shared vertex buffer + coarse->fine ordered index stream (meshoptimizer ^1.1.1 + @gltf-transform ^4.3.0 ALREADY deps) with a {byteOffset->screenError} table; runtime reads up to offset f(screenPx) into a fixed BatchedMesh geometry slot. Full cluster-DAG = MORE code, reject unless continuous NEAR-LOD required. Implementation is BLOCKED on a user product decision (fidelity-vs-simplicity).
