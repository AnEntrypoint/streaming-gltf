---
key: mem-e901f73d41a34f0e-786
ns: default
created: 1780053410959
updated: 1780053410959
---

## Resolved mutable: baseline-anomaly-low-fps

ROOT CAUSE = external GPU contention, NOT a code regression. tasklist shows concurrent automation NOT owned by this project: playwriter bin.js running screenshots against C:/dev/tv8/planet.html, plus a foreign server.js x2, plus stale gm supervisor/wrapper duplicates. measure-fps.mjs docstring confirms two GPU contexts tank both. Renderer is real hardware (RTX3060 D3D11) and max hit 160/111 = GPU CAN reach old numbers; only medians collapse under contention. Absolute steady-state baseline is BLOCKED-EXTERNAL while tv8 automation runs; RELATIVE before/after on the same contended renderer remains valid per harness docstring. Prior authoritative absolute baseline (500=111/1000=83, commit f5bf442) stands as the uncontended reference.
