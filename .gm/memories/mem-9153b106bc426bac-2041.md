---
key: mem-9153b106bc426bac-2041
ns: default
created: 1779901130821
updated: 1779901130821
---

streaming-gltf TRUE ON-GPU POSITION LERP delivered (commits f0c994c, 910f1c7, e7d7193, master, pushed). This finishes the previously-DEFERRED in-shader GPU lerp the original goal explicitly named. CORRECTS prior memory that said GPU lerp was deferred in favor of CPU-only. IMPLEMENTATION (batched-far-tier.js + model-pool.js): the dominant visible tier (far/BatchedMesh, ~890/950 entities) now interpolates position ENTIRELY IN THE VERTEX SHADER. Parallel RGBA32F DataTexture, 2 texels/instance (texel0=pos0.xyz+startTime.w, texel1=pos1.xyz+dur.w), indexed by getIndirectIndex(gl_DrawID). onBeforeCompile (extending the existing gamma patch): declares uLerpTex/uLerpTexW/uNow in <batching_pars_vertex> + a _lerpTexel texelFetch helper; after <batching_vertex>, if(dur>0) batchingMatrix[3].xyz = mix(pos0,pos1,clamp((uNow-startT)/dur,0,1)) — overrides ONLY translation, keeps rotation/scale from setMatrixAt. pool.setTarget takes a GPU fast path (_setFarLerp) for fully-far entities: writes 2 texels, NO CPU mover; uNow pushed once/frame in update(). CPU mover (_movers/_drainMovers) remains the fallback for hero/mid. WITNESSED (gm browser): setTarget on a far entity -> callsDuringFlight40=0 (ZERO BatchedMesh.setMatrixAt calls across 40 frames of motion = interpolation fully on GPU), shader compiled clean. Continuous mid-flight retarget proven snapErr=0 via pool._farLerpState CPU mirror (written only on setTarget, never per-frame; sampled for continuity; pruned on retarget/clear/release). No static regression: 142 FPS @1000. Also fixed measure-fps.mjs CHANNEL=chrome goto stalls (127.0.0.1 + waitUntil:commit). New harness gpu-lerp-check.mjs. Corner cases handled: completion (shader clamp), instant (dur<=0), LOD swap (setGeometryIdAt keeps instanceId, texels survive), release (clearLerp). The BatchedMesh shader-lerp recipe: getIndirectIndex(gl_DrawID) gives instance id, texelFetch a parallel texture, modify batchingMatrix after <batching_vertex>. Supersedes [[streaming-gltf WebGPU + position-update session]] CPU-only claim.
