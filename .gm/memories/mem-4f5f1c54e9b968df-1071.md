---
key: mem-4f5f1c54e9b968df-1071
ns: default
created: 1779805367223
updated: 1779805367223
---

gltf-progressive stress demo CRITICAL bug (fixed commit 490f2b2): enableDrawCallBatching() in draw-call-batching.js monkeypatches pool._getInstancedSlot to return BatchedInstancedSlot stored in pool._geometryBatches, but pool.update() only flushed pool._instancedSlots (empty when batching on) -> FAR-tier batched instance matrices written CPU-side but never GPU-uploaded -> all ~901 instances stuck at origin (0,0,0) stacked invisibly -> looked like models added-then-removed leaving a small group, 26 draws. Fix: enableDrawCallBatching wraps pool.update to flushUpdates() every _geometryBatches entry per frame. ALSO: static entities (matrixAutoUpdate=false) froze _lastScreenPx so LOD never changed with camera; fixed via per-frame pool._cameraMoved flag. window.__debug surface added to stress.js (snapshot/lodHistogram/blankEntities/setCamera/spawnAll) — use page.evaluate(()=>window.__debug.snapshot()) to debug. Diagnosing required reading actual instanceMatrix world positions, not aggregate counts (aggregate metrics LIED: resolved=951 while instAtOrigin=all).
