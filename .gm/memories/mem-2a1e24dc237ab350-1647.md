---
key: mem-2a1e24dc237ab350-1647
ns: default
created: 1779832745978
updated: 1779832745978
---

gltf-progressive BatchedMesh FAR-tier migration plan (in progress, top non-WebGPU win). Survey (browser-218, 500 distinct spawned): 494 distinct FAR/unskinned geometries, 2.2M total verts, 4.5M total indices, ALL indexed, maxVerts 142k per geo. Schemas mostly uniform 'color:4,normal:3,position:3,uv:2' BUT color normalized flag VARIES (16 non-normalized vs 476 normalized) and 2 geos have extra uv1 -> MUST normalize schema before addGeometry: force color Float32 itemSize4 non-normalized (matches a93fa6f color fix), drop uv1 + the per-batch instanceBoundSphere/instanceLodIndex attrs, ensure position/normal/uv/color present. THREE.BatchedMesh r184 API (verified node_modules): new BatchedMesh(maxInstances, maxVerts, maxIndex, material); addGeometry(geo)->geomId (attribute schema FROZEN by first geo, all must match itemSize+normalized+indexed-ness); addInstance(geomId)->instId; setMatrixAt/setColorAt(Color|Vector4 RGBA)/setVisibleAt/setGeometryIdAt(instId,geomId)=SYNCHRONOUS LOD SWAP (kills the async-queue disappear bug); perObjectFrustumCulled=true does CPU per-instance sphere cull in onBeforeRender (REMOVE custom frustum-cull shader patch + instance-transform-texture patch, BatchedMesh supersedes both via USE_BATCHING chunks reading batchingTexture); graceful fallback if ANGLE_multi_draw absent (loops per drawrange, still 1 bind). Size maxVerts>=2.2M (Uint32 idx auto >65535), ~50MB GPU upfront. setInstanceCount/setGeometrySize to grow, optimize() to compact. NO negative-scale matrices. computeBoundingSphere on geo before addGeometry to avoid first-frame scan. Goal: collapse 734 draws (436 far) -> ~1-3, raise 63fps baseline.
