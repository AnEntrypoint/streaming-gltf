---
key: mem-9a196c76a30edff0-1130
ns: default
created: 1779831792742
updated: 1779831792742
---

gltf-progressive MEASURED steady-state baseline (RTX 3060, CHANNEL=chrome, inFlight==0): 500 distinct models = 63.3 FPS median, 734 DRAW CALLS, 3.5M triangles, 13 shader programs, 498 visible/436 far. THE BOTTLENECK IS DRAW CALLS (734) — each distinct FAR asset is its own InstancedBatch=its own draw. Mid-load samples (inFlight>0) lie badly: showed 8-23fps while streaming 500 distinct; ALWAYS wait inFlight==0. measure-fps.mjs CHANNEL=chrome gives the real number (its drawCalls field reads 0 due to autoReset timing — read renderer.info.render.calls right AFTER a manual p.renderer.render() instead). KEY: THREE.BatchedMesh IS available in r184 and IS renderer-native (THREE submits it via internal multi-draw) UNLIKE raw ANGLE_multi_draw which is unreachable through renderer.render(). BatchedMesh can collapse the 436 distinct far-asset draws into ~1-3 = the dominant reachable non-WebGPU win. programs=13 so material sharing is already fine (not a lever). Two Explore agents profiled this; agent FPS estimates (50-100ms loop) were WRONG (contradicted witnessed 1.4ms fast-skip) — trust measurement not agent estimates.
