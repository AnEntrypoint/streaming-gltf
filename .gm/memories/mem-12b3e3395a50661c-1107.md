---
key: mem-12b3e3395a50661c-1107
ns: default
created: 1780344462639
updated: 1780344462639
---

streaming-gltf FULL VRM SUPPORT shipped + witnessed (browser-181, localhost:5180/stress.html via ModelPool). Findings: (1) bake-progressive.mjs PRESERVES the VRM/VRMC extension on the root progressive GLB (extensionsUsed includes VRM + VRMC_materials_mtoon + EP_progressive_lod; sibling LODs are pruned to unskinned). So a baked VRM is fully loadable. (2) @pixiv/three-vrm v3 has NO skeleton-rebind clone (VRM.prototype=[constructor,update]); humanoid/springbone/expression managers bind the ORIGINAL scene bones. FIX in model-pool.js: Asset._vrmOwner lets exactly ONE live entity own+drive the VRM (renders original gltf.scene, this.vrm=srcVrm); all other pooled instances render a _cloneSkinned bind-pose copy with this.vrm=null. Ownership released on Entity.dispose. Added Asset.dispose (VRMUtils.deepDispose) + ModelPool.dispose. Witness: hasVRM true, boneCount 54, owner uses original scene, 2nd instance vrm null + clone, dispose frees owner, 0 console errors. pool.spawn returns a PROXY (Emitter) not the Entity; real entity = proxy.actualEntity, await via proxy ready event (no proxy.ready promise).
