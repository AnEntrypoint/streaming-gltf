---
key: mem-83d1633dc78ca145-1944
ns: default
created: 1779876326233
updated: 1779876326233
---

streaming-gltf LOD-switch stutter FIXED (commit 6d00f26, pushed to AnEntrypoint/streaming-gltf master). Symptom: continuous stutter as LODs changed (esp vertcolor<->textured) at 500-1000 models, and LODs jumped straight lowest<->highest with no middle. ROOT CAUSE (instrumented _applyLod): per-entity _pickMeshLod ran EVERY FRAME for every visible entity with NO dead-band; FPS-controller _lodDistanceScale hunting + continuous orbit-camera motion crossed thresholds and flipped LODs every frame = 17,545 switches over a zoom cycle, 404/2s even with a 'still' camera (the demo orbit-cam keeps moving unless you click it off!), max _applyLod 2685ms; the 3-LOD [0,2,4] mapping forced low<->high jumps (lodHist {0,4} only). 3-PART FIX in model-pool.js: (1) _pickMeshLod hysteresis: +/-18% dead-band keyed on the entity's current ladder index (passed curLod arg) so boundary jitter doesn't flip; (2) per-pool _lodEpoch (in update(), bumped only when _cameraMoved OR |lodScale-last|>0.04) — Entity._update re-picks LOD only when its _lodPickEpoch != pool._lodEpoch, so still camera = ZERO re-picks; (3) _use3LodSystem default flipped to FALSE -> full 5-LOD ladder [0,1,2,3,4] for a smooth gradient. ALSO added (earlier in same edit) a frame-confirm debounce + _lodPending in-flight guard at the pick call site. WITNESSED: still-camera switches 404->0; zoom-cycle switches 17545->10 (~2000x); frame times p50 6.9ms p99 9.9ms max 19.2ms (was 2685ms). NOTE: under an unreachable FPS target the controller correctly drives _lodDistanceScale to SCALE_MIN(0.15) -> everything at LOD0 (range skews low) — that's correct controller behavior, not the bug. TEST METHOD: instrument Entity prototype _applyLod (count+time), MUST disable orbit-cam+zoom-cycle checkboxes for a true still-camera test, sample frame intervals via rAF loop. Browser-verb waitUntil:'commit' + waitForFunction (not domcontentloaded, which times out on CDN three+workers cold-load).
