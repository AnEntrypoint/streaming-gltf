---
key: mem-5513dc9c6da3ac46-1186
ns: default
created: 1779833919128
updated: 1779833919128
---

gltf-progressive KEY PERF FINDING (commit a5888d1): at 500 distinct models the bottleneck is TRIANGLE/FILL throughput (~3.5M triangles), NOT draw calls. Proven by: wiring THREE.BatchedMesh FAR tier (collapses 437 far draws -> ~1, verified browser-238 10geoms->1draw) gave 63.0fps steady-state = IDENTICAL to the per-asset InstancedBatch baseline (63fps). Draw-call reduction = ZERO fps gain on RTX 3060. So ignore draw-call-count optimizations (multi-draw, batching) for this workload — they're already non-binding. The real levers are: (1) fewer far-tier TRIANGLES — far geometries avg ~4500 verts (2.2M/494), max 142k; far LOD decimation isn't aggressive enough; push far models to lower-poly LODs / re-bake with more aggressive decimation ratios. (2) fill/overdraw reduction. BatchedFarTier module exists + pool.useBatchedFarTier opt-in (default OFF) for genuinely draw-call-bound workloads. measure-fps.mjs CHANNEL=chrome can sample mid-warmup (showed 47fps visible258) vs true steady (63fps visible498 far437) — trust the far-heavy high-visible sample. Two Explore agents BOTH wrongly predicted draw calls as the dominant win; only direct before/after measurement settled it.
